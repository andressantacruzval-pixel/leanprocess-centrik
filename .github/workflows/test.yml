name: Prueba básica
on: [push]

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Clonar el repositorio
        uses: actions/checkout@v4

      - name: Verificar conexión del runner
        run: echo "El runner funciona correctamente"
