# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment
.env
.env.local
.env.*.local

# Referencia local — documentación interna, no versionar
Referencia/
Skills/
planes de paquetes de compra stripe/

# MCP (Model Context Protocol) — nunca versionar: contienen access tokens
.mcp.json
.mcp.*.json
mcp.json
.claude/settings.local.json
.cursor/mcp.json

# Contextos volcados de otros proyectos — no pertenecen a este repo
CRM CONTEXT.md
*CONTEXT.md
